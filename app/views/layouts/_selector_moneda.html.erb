<%# este partial requiere los siguientes parametros
  moneda: [valores existentes]
  name: "name del select" (opcional)
%>

<% monedas = ['ARS', 'USD'] %>
<label>Moneda</label>
<select id="selector_moneda" class="form-control" <%= defined?(seleccion) ? seleccion : nil %>
  <%= defined?(name) ? "name=#{name}" : '' %> >
  <% monedas.each do |m| %>
    <option value="<%= m %>" <%= moneda_seleccionada(m, moneda) %>><%= m %></option>
  <% end %>
</select>
